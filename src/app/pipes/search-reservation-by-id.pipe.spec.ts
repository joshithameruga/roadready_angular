import { SearchReservationByIdPipe } from './search-reservation-by-id.pipe';

describe('SearchReservationByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchReservationByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
