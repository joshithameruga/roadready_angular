import { SearchCarByIdPipe } from './search-car-by-id.pipe';

describe('SearchCarByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCarByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
