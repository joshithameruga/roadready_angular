import { SearchPaymentByIdPipe } from './search-payment-by-id.pipe';

describe('SearchPaymentByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPaymentByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
