import { SearchFeedbackPipe } from './search-feedback.pipe';

describe('SearchFeedbackPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFeedbackPipe();
    expect(pipe).toBeTruthy();
  });
});
